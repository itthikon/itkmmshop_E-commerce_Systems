# คู่มือระบบ Authentication

## บัญชีทดสอบ

ระบบมีบัญชีทดสอบ 3 ประเภท:

### 🔑 Admin (ผู้ดูแลระบบ)
```
Email: admin@itkmmshop22.com
Password: admin123
```
**สิทธิ์:**
- จัดการระบบทั้งหมด
- ดูรายงานการเงินและการวิเคราะห์
- จัดการผู้ใช้และสินค้า
- จัดการคำสั่งซื้อทั้งหมด

### 👔 Staff (พนักงาน)
```
Email: staff@itkmmshop22.com
Password: staff123
```
**สิทธิ์:**
- สร้างคำสั่งซื้อให้ลูกค้า
- จัดการคำสั่งซื้อ
- ดูข้อมูลสินค้า
- อัปเดตสถานะคำสั่งซื้อ

### 🛒 Customer (ลูกค้า)
```
Email: customer@itkmmshop22.com
Password: customer123
```
**สิทธิ์:**
- ซื้อสินค้า
- ดูประวัติการสั่งซื้อ
- จัดการโปรไฟล์
- ติดตามสถานะคำสั่งซื้อ

## การใช้งาน

### เข้าสู่ระบบ
1. ไปที่หน้า Login: `http://localhost:3000/login`
2. กรอกอีเมลและรหัสผ่าน
3. ระบบจะ redirect ตาม role:
   - Admin → `/admin`
   - Staff → `/staff`
   - Customer → `/`

### สมัครสมาชิก
1. ไปที่หน้า Register: `http://localhost:3000/register`
2. กรอกข้อมูล:
   - ชื่อ-นามสกุล (required)
   - อีเมล (required)
   - รหัสผ่าน (required, อย่างน้อย 6 ตัวอักษร)
   - เบอร์โทรศัพท์ (optional, 10 หลัก)
   - เพศ (optional)
   - วันเกิด (optional)
3. บัญชีใหม่จะมี role เป็น "customer" โดยอัตโนมัติ

## API Endpoints

### Login
```bash
POST /api/auth/login
Content-Type: application/json

{
  "email": "admin@itkmmshop22.com",
  "password": "admin123"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "admin@itkmmshop22.com",
      "first_name": "Admin",
      "last_name": "User",
      "role": "admin"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### Register
```bash
POST /api/auth/register
Content-Type: application/json

{
  "email": "newuser@example.com",
  "password": "password123",
  "first_name": "John",
  "last_name": "Doe",
  "phone": "0812345678"
}
```

### Get Profile
```bash
GET /api/auth/profile
Authorization: Bearer <token>
```

### Logout
```bash
POST /api/auth/logout
Authorization: Bearer <token>
```

## การจัดการรหัสผ่าน

### รีเซ็ตรหัสผ่านบัญชีทดสอบ
```bash
cd backend
node update-passwords.js
```

### เพิ่มบัญชีพนักงานใหม่
```bash
cd backend
node add-staff-user.js
```

## Security Features

- ✅ Password hashing with bcrypt (10 rounds)
- ✅ JWT token authentication
- ✅ Rate limiting on auth endpoints (5 attempts per 15 minutes)
- ✅ Input validation with Joi
- ✅ XSS protection
- ✅ SQL injection prevention
- ✅ CORS configuration

## หมายเหตุ

⚠️ **สำคัญ:** บัญชีทดสอบเหล่านี้ใช้สำหรับการพัฒนาเท่านั้น  
ในโหมด production ควร:
1. เปลี่ยนรหัสผ่านทั้งหมด
2. ลบหรือปิดการใช้งานบัญชีทดสอบ
3. ใช้รหัสผ่านที่แข็งแรงและซับซ้อน
4. เปิดใช้งาน 2FA (Two-Factor Authentication)
