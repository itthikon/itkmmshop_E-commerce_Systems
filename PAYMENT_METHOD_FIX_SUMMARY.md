# แก้ไขปัญหา: Payment Method = undefined

## 🔍 ปัญหาที่พบ

จาก console log:
```
Payment Method: undefined
Payment Method Type: undefined
Should Show Upload: false
```

**สาเหตุ:** ตาราง `orders` ไม่มี column `payment_method` เลย!

## ✅ การแก้ไข

### 1. เพิ่ม Column ในฐานข้อมูล
```bash
cd backend
node add-payment-method-column.js
```

เพิ่ม column:
- ชื่อ: `payment_method`
- ประเภท: ENUM('bank_transfer', 'promptpay', 'cod', 'cash', 'credit_card')
- ค่าเริ่มต้น: 'bank_transfer'

### 2. แก้ไข Backend Controller
**ไฟล์:** `backend/controllers/orderController.js`

เพิ่ม validation สำหรับ `payment_method`:
```javascript
payment_method: Joi.string().valid('bank_transfer', 'promptpay', 'cod', 'cash', 'credit_card').default('bank_transfer')
```

### 3. แก้ไข Order Model
**ไฟล์:** `backend/models/Order.js`

- เพิ่ม `payment_method` ใน destructuring
- เพิ่ม `payment_method` ใน INSERT query
- ส่งค่า `payment_method` ไปยัง database

### 4. แก้ไข Frontend Checkout
**ไฟล์:** `frontend/src/pages/customer/Checkout.js`

เพิ่มการส่ง `payment_method` ใน API request:
```javascript
const orderResponse = await api.post('/orders', {
  cart_id: cartData.id,
  payment_method: formData.payment_method,  // ← เพิ่มบรรทัดนี้
  guest_name: formData.shipping_name,
  // ...
});
```

## 🎯 ผลลัพธ์

หลังจากแก้ไข:
1. ✅ Database มี column `payment_method`
2. ✅ Backend รับและบันทึก `payment_method`
3. ✅ Frontend ส่ง `payment_method` ไปยัง backend
4. ✅ Order Confirmation จะได้รับ `payment_method` จาก API
5. ✅ ช่องอัปโหลดสลิปจะแสดงถูกต้อง

## 📝 ทดสอบ

### ขั้นตอนที่ 1: สั่งซื้อใหม่
1. ไปที่หน้าสินค้า
2. เพิ่มสินค้าลงตะกร้า
3. ไปที่ Checkout
4. **เลือก Payment Method: "โอนเงินผ่านธนาคาร"**
5. กรอกข้อมูลที่อยู่
6. กดยืนยันการสั่งซื้อ

### ขั้นตอนที่ 2: ตรวจสอบ Console
เปิด Browser Console (F12) ควรเห็น:
```
=== ORDER DATA DEBUG ===
Order ID: 10
Payment Method: bank_transfer  ← ไม่ใช่ undefined แล้ว!
Payment Method Type: string
Should Show Upload: true       ← เปลี่ยนเป็น true!
========================
```

### ขั้นตอนที่ 3: ตรวจสอบหน้าเว็บ
ควรเห็น:
- ✅ ข้อมูลบัญชีธนาคาร
- ✅ ช่องอัปโหลดสลิป
- ✅ ปุ่ม "เลือกไฟล์" หรือ Drag & Drop area

## ⚠️ หมายเหตุสำคัญ

**Order เก่าที่สร้างก่อนแก้ไข:**
- จะมี `payment_method = 'bank_transfer'` (ค่า default)
- ถ้าต้องการแก้ไข ต้อง UPDATE ใน database ด้วยตนเอง

**Order ใหม่ที่สร้างหลังแก้ไข:**
- จะมี `payment_method` ตามที่เลือกในหน้า Checkout
- ช่องอัปโหลดสลิปจะแสดงถูกต้อง

## 🚀 ขั้นตอนถัดไป

1. **Restart Backend Server** (ถ้ายังไม่ได้ restart)
   ```bash
   cd backend
   npm start
   ```

2. **Refresh Frontend** (กด F5)

3. **สั่งซื้อใหม่** เพื่อทดสอบ

4. **ตรวจสอบ** ว่าช่องอัปโหลดสลิปแสดงแล้ว

---

**สถานะ:** ✅ แก้ไขเสร็จสมบูรณ์
**วันที่:** 2026-01-01
**ไฟล์ที่แก้ไข:** 4 ไฟล์
**Database Migration:** เสร็จสิ้น
